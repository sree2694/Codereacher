import React from 'react';

const HTMLCSSPage = () => {
    return (
        <div className="technology-page">
            <h1>HTML & CSS</h1>
            <p>HTML and CSS are foundational technologies for building the structure and styling of web pages.</p>
        </div>
    );
};

export default HTMLCSSPage;
